"""
Shared helper functions. Examples:
A generic pagination function that takes a query and returns
(items, total_count).
A filter‑builder for search endpoints.
An upsert() helper for bulk inserts.
"""
